---
en:
  date:
    formats:
      default: "%m/%d/%Y"
      with_weekday: "%a %m/%d/%y"

  time:
    formats:
      default: "%a, %b %-d, %Y at %r"
      date: "%b %-d, %Y"
      short: "%B %d"

  titles:
    application: "TABC: CR"
    dashboards:
      show: Dashboard
    sessions:
      create: Log In
      new: Log In
    users:
      create: Sign Up
      new: Sign Up

  users_mailer:
    request_pin:
      subject: "TABC: CR — Pin Request"
    welcome:
      subject: "TABC: CR — Welcome"

  grape:
    errors:
      messages:
        presence: is required
        id: is not a valid id
        entity: does not exist
        email: is not a valid email address

  notices:
    users:
      create:
        new: User registered successfully.
        invite: User successfully invited.
